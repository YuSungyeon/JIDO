spring:
  application:
    name: JIDO_

  datasource:
    url: jdbc:mysql://localhost:3306/LOADMAP_PROGRAM
    username: root
    password: dbtjddus12
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080