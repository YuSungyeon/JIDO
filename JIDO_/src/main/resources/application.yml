spring:
  datasource:
    url: jdbc:mysql://localhost:3306/roadmap_db
    username: root
    password: whgkdms0506
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate # 테이블이 엔티티 구조와 일치하는지만 확인

    show-sql: true

    properties:
      hibernate:
        format_sql: true


  server:
    port: 8080